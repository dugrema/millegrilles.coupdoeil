#!/usr/bin/env node

/**
 * Module dependencies.
 */

const app = require('../app');
const websocketsapp = require('../websocketsapp');
const debug = require('debug')('api:server');
const https = require('https');
const FS = require("fs");
const Path = require('path');
// const Spdy = require('spdy');
// const http2 = require('http2');
const io = require('socket.io');
const rabbitMQ = require('../routes/res/rabbitMQ')

/**
 * Get port from environment and store in Express.
 */

var port = normalizePort(process.env.PORT || '443');
const hostIp = process.env.HOST || 'dev2.maple.mdugre.info';
const key_path = process.env.PRIVKEY;
const cert_path = process.env.CERT;
const nomMilleGrille = process.env.MG_NOM_MILLEGRILLE;

/**
 * Create SPDY/http2 server.
 */

const config = {
    hostIp: hostIp,
    cert: FS.readFileSync(cert_path),
    key: FS.readFileSync(key_path)
};

const wss_server = io();
const server = https.createServer(config, app).listen(port, () => {
  wss_server.listen(server);
  wss_server.on('connection', (socket) => {
    console.info("Connexion WSS id: " + socket.id + ", remoteAddress: " + socket.conn.remoteAddress);
    websocketsapp.addSocket(socket);
  });

  console.log('Server is listening at https://localhost:' + port + '. Ctrl^C to stop it.');
});


// Connexion a RabbitMQ
let mqConnectionUrl = process.env.MG_MQ_URL || 'amqps://mq:5673/' + nomMilleGrille;
rabbitMQ.singleton.connect(mqConnectionUrl);


/**
 * Normalize a port into a number, string, or false.
 */

function normalizePort(val) {
  var port = parseInt(val, 10);

  if (isNaN(port)) {
    // named pipe
    return val;
  }

  if (port >= 0) {
    // port number
    return port;
  }

  return false;
}
